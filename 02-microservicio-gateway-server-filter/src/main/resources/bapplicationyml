spring:
  application:
    name: gateway-server
  profiles:
    active:
    - dev 
##si no tiene docker cambia la ruta de eureka de  
##    - docker            
  cloud:
    config:
##SIN DOCKER
      uri: http://config-server:8889
##CON DOCKER ,parece que sin docker¿? tambien va
##        uri: host.docker.internal:config-server
##ESTE TIENE PRIORIDAD SOBRE EL APPCONFIG
server:
  port: 8090